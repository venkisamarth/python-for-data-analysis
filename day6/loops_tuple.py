# loops through a tuple 
# you can loop throught the tuple items by using a for loop
# iterable through the  tuple itesm by using  fro loop

iterable=("apple",'banana',"cherry")
for x in iterable:
    print(x)

# loop through the index numbers

# you can also loop through the iterms by refering  to therir numbers
# us the range() and len() functions to create a suitable iterable

thistuple=("apple","banana","cherry")
for i in range(len(thistuple)):
      print(thistuple)

# using while loo
i=0 
while i <len(thistuple):
     print(thistuple[i])

     i=i+1

    


    


